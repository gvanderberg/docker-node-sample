extends ../layouts/desktop

block page 
    - var menu = "blog"
block content
    .container
        .row
            .page-header
                h2 #{title} <small>#{blog.createdAt.toDateString()}</small>
            .col-sm-8.col-xs-12
              p #{blog.body}