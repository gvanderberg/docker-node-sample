var gulp = require("gulp");
var bower = require("gulp-bower");

gulp.task("default", () => { });

gulp.task("bower", () => { 
    return bower();
});

gulp.task("build", () => { });

gulp.task("clean", () => { });